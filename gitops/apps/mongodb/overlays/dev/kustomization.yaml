apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mongo-dev

resources:
  - ../../base
  - mongo-cluster.yaml

commonAnnotations:
  environment: dev
  managed-by: openshift-gitops
