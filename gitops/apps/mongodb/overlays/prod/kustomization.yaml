apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mongo-prod

resources:
  - ../../base
  - mongo-cluster.yaml

commonAnnotations:
  environment: prod
  managed-by: openshift-gitops
