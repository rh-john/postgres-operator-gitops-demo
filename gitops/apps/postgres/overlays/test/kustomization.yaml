apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pg-test

resources:
  - ../../base
  - pg-cluster.yaml

commonAnnotations:
  environment: test
  managed-by: openshift-gitops
