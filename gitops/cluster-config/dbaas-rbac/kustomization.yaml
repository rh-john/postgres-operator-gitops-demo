apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DBaaS RBAC – Crunchy PGO + Percona MongoDB roles.
# Separate from the original rbac-cluster (Zalando) – coexists safely.

resources:
  - postgres-user-role-crunchy.yaml
  - mongo-user-role.yaml

commonLabels:
  demo: dbaas
  managed-by: gitops
