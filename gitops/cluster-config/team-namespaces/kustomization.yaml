apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces.yaml

# Note: We don't add commonLabels or commonAnnotations here because:
# 1. They would conflict with OpenShift-managed labels
# 2. Each namespace already has its own labels defined
# 3. ArgoCD adds its own management labels automatically
