apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dbaas-namespaces
  namespace: openshift-gitops
  labels:
    app: dbaas-namespaces
    demo: dbaas
    managed-by: cluster-admin
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: cluster-infrastructure

  source:
    repoURL: https://github.com/john-johansson/postgres-operator-gitops-demo
    targetRevision: feature/dbaas-operators
    path: gitops/cluster-config/dbaas-namespaces

  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-gitops

  syncPolicy:
    automated:
      prune: false   # Never auto-delete namespaces
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
